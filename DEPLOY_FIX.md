I fixed the TypeScript error by removing the redundant `if (global.setInterval)` check. 
You can now redeploy your application on Vercel. 
Check the Deployments tab; a new build should start automatically.
